<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Connexion - XSS VADI</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <div class="login-container">
    <div class="login-card card">
      <div class="card-body">
        <div class="login-header">
          <div class="login-logo">&#128274;</div>
          <h1 class="login-title">XSS VADI</h1>
          <p class="login-subtitle">Demonstration pedagogique des failles XSS</p>
        </div>

        <% if (error) { %>
          <div class="alert alert-danger">
            <span class="alert-icon">&#9888;</span>
            <div class="alert-content">
              <p class="alert-message"><%= error %></p>
            </div>
          </div>
        <% } %>

        <form method="POST" action="/login">
          <div class="form-group">
            <label for="username" class="form-label">Nom d'utilisateur</label>
            <input
              type="text"
              class="form-input"
              id="username"
              name="username"
              placeholder="Entrez votre prenom"
              required
              autocomplete="username"
            >
            <span class="form-hint">Utilisez votre prenom ou un pseudonyme</span>
          </div>

          <div class="form-group">
            <label for="password" class="form-label">Mot de passe</label>
            <input
              type="password"
              class="form-input"
              id="password"
              name="password"
              placeholder="Entrez un mot de passe"
              required
              autocomplete="current-password"
            >
            <span class="form-hint text-danger">N'utilisez JAMAIS votre vrai mot de passe !</span>
          </div>

          <button type="submit" class="btn btn-primary btn-lg w-full">
            Se connecter
          </button>
        </form>

        <hr>

        <div class="card card-primary">
          <div class="card-body">
            <h5 class="text-primary mb-md">&#8505; Informations</h5>
            <ul class="checklist">
              <li class="checklist-item">Eleves : Entrez votre prenom + n'importe quel mot de passe</li>
              <li class="checklist-item">Cette app est une demo pedagogique volontairement vulnerable</li>
            </ul>
          </div>
        </div>

        <div class="alert alert-warning mt-lg">
          <span class="alert-icon">&#9888;</span>
          <div class="alert-content">
            <p class="alert-title">Avertissement</p>
            <p class="alert-message">Cette application est VOLONTAIREMENT VULNERABLE. Usage pedagogique uniquement en environnement controle.</p>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script src="/js/app.js"></script>
</body>
</html>
