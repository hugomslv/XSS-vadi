<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> - XSS VADI</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <%- include('partials/navbar') %>

  <div class="alert alert-warning mb-0 text-center" role="alert">
    <strong>⚠️ AVERTISSEMENT :</strong> Cette application est VOLONTAIREMENT VULNÉRABLE à des fins pédagogiques. Usage local uniquement.
  </div>

  <main class="container my-5">
    <h1 class="mb-4">📚 <%= title %></h1>

    <div class="alert alert-info">
      <h5>Bienvenue dans la démonstration XSS !</h5>
      <p class="mb-0">
        Chaque chapitre présente un concept de sécurité web avec des exercices pratiques.
        Cliquez sur un chapitre pour commencer.
      </p>
    </div>

    <div class="row">
      <% chapters.forEach((chapter, index) => { %>
        <div class="col-md-6 mb-3">
          <div class="card h-100">
            <div class="card-body">
              <h5 class="card-title">
                <span class="badge bg-danger"><%= chapter.order %></span>
                <%= chapter.title.replace(/^Chapitre \d+ : /, '') %>
              </h5>
              <p class="card-text text-muted small">
                <%= chapter.instructions || 'Cliquez pour accéder au chapitre' %>
              </p>
              <a href="/chapter/<%= chapter.id %>" class="btn btn-danger">
                Accéder au chapitre
              </a>
            </div>
          </div>
        </div>
      <% }) %>
    </div>
  </main>

  <%- include('partials/footer') %>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
